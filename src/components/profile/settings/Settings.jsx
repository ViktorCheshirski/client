import React, {useState} from 'react';
import {useDispatch, useSelector} from "react-redux";
import {} from "../../../actions/file";;

const Settings = () => {
    const dispatch = useDispatch()

    return (
        <div>        
            <div>Настройки</div>
        </div>
    );
};

export default Settings;